@model List<ProcessorFilterModel>

@* <h5>Brands</h5>
    @foreach (var category in Model)
    {
    @* <div class="checkbox mb-3">
    <label><input type="checkbox" value="@category.Name">@category.Name</label>
    </div> *@
@* <h5>Brands</h5>
    <div class="list-group">


    @foreach (var item in Model)
    {

    <div class="form-check" style="background-color: #F0F8FF;">
    <input class="form-check-input" type="checkbox" value="" id="defaultCheck1">
    <label class="form-check-label" for="defaultCheck1">
    @item.FilterName
    </label>

    </div>
    } *@
@* <a asp-controller="compare" asp-action="filter" class="btn btn-primary btn-sm">Fitrele</a> *@
<table class="table table-striped table table-bordered">
    @using (Html.BeginForm("ProcessorFilterList", "Compare", FormMethod.Post))
    {
        for (int i = 0; i < Model.Count; i++)
        {

            <tr>
                <td>
                    @Html.HiddenFor(m=>Model[i].ProcessorName)
                    @Html.CheckBoxFor(m=>Model[i].IsSelected)
                    @Html.DisplayFor(m=>Model[i].ProcessorName)
                </td>
            </tr>

        }
        <input type="submit" value="Fitrele" class="btn btn-primary btn-sm" />
    }
</table>
@* <form asp-controller="Admin" asp-action="ProductEdit" method="POST" enctype="multipart/form-data">
    @for (int i = 0; i < Model.Count(); i++){

    <div class="form-check" style="background-color: #F0F8FF;">
    <input class="form-check-input" type="checkbox" value="" id="defaultCheck1">
    <input type="hidden" name="LaptopMarka" value="@Model[i].FilterName">
    <label class="form-check-label" for="defaultCheck1">
    @Model[i].FilterName
    </label>
    </div>
    } *@